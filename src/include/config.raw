<?

  /* Cetral configuration-file for the koch-suite
   * by Michael Lestinsky <michael@zaphod.rhein-neckar.de>, Oktober 2000
   * $Id: config.raw,v 1.6 2001/05/16 00:21:26 michael Exp $
   */

  $DB_SERVER   = "<%%DB_SERVER%%>";
  $DB_USER     = "<%%DB_USER%%>";
  $DB_PASSWORD = "<%%DB_PASSWORD%%>";
  $DB_NAME     = "<%%DB_NAME%%>";

  $DEBUG_FILE  = "/tmp/koch-suite-debug";

  $DEFAULT_LANG = "de";

  $kochsuite_ver = "0.5-pre1";

  /* Connect to the Database */

  if (get_cfg_var(mysql.allow_persistent) == 1) {
    if (! @mysql_pconnect($DB_SERVER, $DB_USER, $DB_PASSWORD)) {
      $mysql_error = mysql_error();
    }
  } else {
    if (! @mysql_connect($DB_SERVER, $DB_USER, $DB_PASSWORD)) {
      $mysql_error = mysql_error();
    }
  }

?>
