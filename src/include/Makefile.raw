#!/usr/bin/make

# This file contains the substitution-rules for "sed"

# Space-separated list of all "raw" php-files.
PHP_FILES_UNCONF=	config.inc functions.inc output.inc
SUBDIRS=		

CONF_FILE=		../config.sed

all: build
	@#make subdir TRGT=all

build: ${PHP_FILES_UNCONF}

${PHP_FILES_UNCONF}: ${PHP_FILES_UNCONF:%.inc=%.raw}
	sed -f ${CONF_FILE} ${@:%.inc=%.raw} > ${@}

subdir:
	@(for i in ${SUBDIRS} ; do\
	echo "====> ${THISDIR}/$$i";\
	cd $$i; \
	${MAKE} ${TRGT} THISDIR=${THISDIR}/$$i; \
	echo "<==== ${THISDIR}";\
	done )

clean:
	@#make subdir TRGT=clean
	rm -f ${PHP_FILES_UNCONF}

distclean:

install: build
	( [ -d "<%%PHP_LIBS%%>" ] || \
	install -d -g <%%GROUP%%> -m 0755 -o <%%OWNER%%> <%%PHP_LIBS%%> )
	install -c -g <%%GROUP%%> -m 0644 -o <%%OWNER%%> ${PHP_FILES_UNCONF} <%%PHP_LIBS%%>
